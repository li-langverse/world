# Repo: li-cursor-agents

Cursor SDK local agent runner for li-langverse swarm.

## Agent-kit

- Sync: `./scripts/sync-agent-kit.sh` (overlay `overlays/li-cursor-agents`)
- After roadmap `agent-kit/manifest.toml` bump: install here + run **agent_kit_maintainer** across org

## Preflight

`benchmarks/scripts/agent-briefing.py` → `recommended_agents` / `heap_plan`
