# Repo scope: **lip** (issue feature planner)

Parent: [issue-feature-planner.md](../issue-feature-planner.md)

## This repo

Package manager, registry, lockfile, publish — plans reference **PH-8b–8d** in master plan.

## Feature plan must include

- `li.toml` / `li.lock` schema impact
- Downstream `li-toolchain.toml` bumps
- `lit` coverage gate on publish

```bash
gh issue list --repo li-langverse/lip --state open
```
